# Local model folder

Place the quantized Mistral GGUF here as:

```text
models/mistral-7b-instruct-v0.2.Q4_K_M.gguf
```

The model file is intentionally ignored by Git. Build the LLM image only after the file exists.

Download helper:

```powershell
.\scripts\download-mistral.ps1
```
